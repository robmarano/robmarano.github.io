\documentclass[aspectratio=169]{beamer}

\usetheme{Madrid}
\usecolortheme{default}

\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{hyperref}

\title{ECE 251: Computer Architecture}
\subtitle{Week 11: Pipelining \& Hazard Resolution}
\author{Prof Rob Marano}
\date{Spring 2026}

% Cooper Union Brand Identity
\definecolor{CooperRed}{HTML}{8A1538}
\definecolor{CooperDark}{HTML}{5C0E25}
\setbeamercolor{structure}{fg=CooperRed}
\setbeamercolor{palette primary}{bg=CooperRed,fg=white}
\setbeamercolor{palette secondary}{bg=CooperRed,fg=white}
\setbeamercolor{palette tertiary}{bg=CooperDark,fg=white}
\setbeamercolor{title in head/foot}{bg=CooperDark,fg=white}
\logo{\includegraphics[height=1cm]{../../images/cooper_union_logo.png}}
\titlegraphic{\includegraphics[height=1.5cm,keepaspectratio]{../../images/cooper_union_logo.png}}

\begin{document}

\begin{frame}
\titlepage
\end{frame}

\begin{frame}{1. Transition to True Hardware Pipelining}
\begin{itemize}
    \item \textbf{Core Concept}: Multiple instructions are actively executed simultaneously across discrete hardware zones (Overlap Execution).
    \item \textbf{The Goal}: Process $N$ instructions in $N$ clock cycles, driving Cycles Per Instruction ($CPI$) down to $1.0$.
    \item \textbf{The Reality}: The global machine clock is constrained strictly by the slowest hardware stage (e.g., Memory retrieval).
    \item \textbf{The Trade-off}: Throughput dramatically increases over Single-Cycle, but the physical duration applied to an individual isolated instruction (Latency) slightly increases due to pipeline logic overhead.
\end{itemize}
\end{frame}

\begin{frame}{2. The Amdahl's Law Limit}
\begin{itemize}
    \item Pipelining overlaps execution, scaling speedup proportionally by hardware stages $k$.
    \item \textbf{Amdahl's Formula}: $\text{Speedup} = \frac{1}{(1 - p) + \frac{p}{k}}$
    \item \textbf{MIPS Assembly Mapping ($p$)}: If $p = 0.8$, $80\%$ of instructions execute flawlessly in parallel (e.g. bypassed ALU math, safely predicted branches).
    \item \textbf{The Penalty Mapping ($1-p$)}: $20\%$ of execution triggers a hard structural stop via \textbf{Hazards} (e.g., \texttt{lw} memory stalls, pipeline flushes).
    \item \textbf{The Mathematical Ceiling}: Even as pipelining stages approach infinity ($k \to \infty$), the maximum physical speedup strictly caps at $\frac{1}{1-p}$. Hazards physically dictate CPU limits!
\end{itemize}
\end{frame}

\begin{frame}{2b. Resolving Break-Even Parallelization}
\begin{itemize}
    \item \textbf{Problem}: An 8-stage pipeline ($k=8$) \textit{must} achieve a $4.0\times$ speedup over a single-cycle implementation. Calculate the fraction of instructions ($p$) that must be completely free of hazards to hit this target.
    \item \textbf{Algebraic Execution}:
    \begin{align*}
        4.0 &= \frac{1}{(1-p) + \frac{p}{8}} \\
        (1-p) + \frac{p}{8} &= 0.25 \\
        1 - \frac{7p}{8} &= 0.25 \\
        \frac{7p}{8} &= 0.75 \implies 7p = 6 \implies p \approx 85.71\%
    \end{align*}
    \item \textbf{Result}: The compiler and hardware forwarders must orchestrate a flawless bypass environment for $85.71\%$ of executed code.
\end{itemize}
\end{frame}

\begin{frame}{3. Establishing Pipeline Registers}
Because data must be preserved securely while instructions shift into the next functional stage, we partition the datapath using large $D$-Flip-Flop arrays called \textbf{Pipeline Registers}.
\vspace{0.2cm}
\begin{enumerate}
    \item \textbf{IF/ID}: Holds fetched 32-bit instruction payload and sequential PC.
    \item \textbf{ID/EX}: Holds Registry data ($A$, $B$), Sign-Extended Immediate, and target Register Addresses.
    \item \textbf{EX/MEM}: Holds computed ALU output and target Write Destination address.
    \item \textbf{MEM/WB}: Holds loaded RAM payload preparing for destination registry push.
\end{enumerate}
\end{frame}

\begin{frame}{3b. Pipelined Control Propagation}
\begin{itemize}
    \item \textbf{The Reality}: The Decoder calculates all 9 multiplexer bits (e.g. \texttt{RegDst}, \texttt{MemWrite}) inside the \texttt{ID} stage.
    \item \textbf{The Risk}: We \textbf{cannot} broadcast these control signals globally! Overlapping instructions would instantly cross-corrupt each other.
    \item \textbf{The Solution}: Control logic natively ''rides the pipe''. The 9 bits are wired into the \texttt{ID/EX} pipeline register boundary explicitly alongside the data.
    \item \textbf{Asynchronous Unpacking}: As instructions progress, Control pieces correctly split off. 4 bits split locally into the ALU (EX stage), 3 bits buffer down the boundary and split into Memory (MEM stage), and the final 2 bits survive until WriteBack.
\end{itemize}
\end{frame}

\begin{frame}[fragile]{4. SystemVerilog: The Pipeline Register Stage}
\begin{verbatim}
// SystemVerilog ID/EX Pipeline Register DFF
module id_ex_reg (input  logic        clk, reset,
                  input  logic        RegWriteD, MemWriteD, // Control
                  input  logic [31:0] RD1D, RD2D, ImmD,     // Data
                  input  logic [4:0]  RsD, RtD, RdD,        // Targeting
                  output logic        RegWriteE, MemWriteE, 
                  output logic [31:0] RD1E, RD2E, ImmE,
                  output logic [4:0]  RsE, RtE, RdE);

  always_ff @(posedge clk or posedge reset) begin
    if (reset) begin
      RegWriteE <= 0; // Evaluate No-Op flush...
    end else begin
      RegWriteE <= RegWriteD;
      RD1E      <= RD1D;
      RsE       <= RsD;       // Locked for Hazard evaluation downstream
    end
  end
endmodule
\end{verbatim}
\end{frame}

\begin{frame}{5. Data Hazards: RAW (Read-After-Write)}
\begin{itemize}
    \item \textbf{Data Hazards} manifest when a physical target instruction attempts to consume algebraic variables generated by an older instruction still evaluating inside the pipeline.
    \item Example (Read-After-Write RAW calculation bypass):
    \begin{itemize}
        \item \texttt{add \$s0, \$t0, \$t1} $\to$ \texttt{\$s0} locked for modification via ALU
        \item \texttt{sub \$t2, \$s0, \$t3} $\to$ Immediately demands \texttt{\$s0} for subtraction input
    \end{itemize}
    \item \textbf{Solution}: We do not wait for the fifth stage (WB). The ALU calculates \texttt{\$s0} locally in stage 3 (EX). We algorithmically \textbf{Forward} (bypass) the data natively backwards utilizing multiplexers.
    \item \textbf{Physical Stalling (The Bubble)}: If the data isn't algebraically ready (e.g. a memory load \texttt{lw} taking 4 stages), forwarding is physically impossible in time. We must stall the pipeline by synthesizing an inert algorithmic bubble! \textit{Did you know? MIPS architecture natively invokes the raw code} \texttt{0x00000000} \textit{translated identically as} \texttt{sll \$zero, \$zero, 0} \textit{to safely delay execution 1 cycle without corrupting registries!}
\end{itemize}
\end{frame}

\begin{frame}{5b. Mathematical Pairs of Hazard Conditions}
Before physically bypassing logic, the CPU must mathematically detect exact execution collisions natively sitting inside the \texttt{EX} hardware stage:
\begin{itemize}
    \item \textbf{EX Hazards (1-Cycle Delay)}: The physically preceding instruction modifies a register we demand right now.
    \begin{itemize}
        \item \texttt{1a: EX/MEM.Rd == ID/EX.Rs}
        \item \texttt{1b: EX/MEM.Rd == ID/EX.Rt}
    \end{itemize}
    \item \textbf{MEM Hazards (2-Cycle Delay)}: An older instruction sitting securely two boundary registers deep is modifying a register we need.
    \begin{itemize}
        \item \texttt{2a: MEM/WB.Rd == ID/EX.Rs}
        \item \texttt{2b: MEM/WB.Rd == ID/EX.Rt}
    \end{itemize}
    \item \textit{Caveat}: The Hardware physically verifies \texttt{RegWrite == 1} and \texttt{Rd != \$0} before forcefully throwing multiplexer routing pins!
\end{itemize}
\end{frame}

\begin{frame}[fragile]{6. SystemVerilog: The Hazard / Forwarding Unit}
\begin{verbatim}
module hazard_unit (input  logic [4:0] RsE, RtE,   // Current Stage Src Regs
                    input  logic [4:0] WriteRegM,  // Prev Stage Dest Reg
                    input  logic       RegWriteM,  // Validation Flag
                    output logic [1:0] ForwardAE, ForwardBE);

  // Forwarding evaluation for ALU Source Port A
  always_comb begin
    if ((RegWriteM) && (WriteRegM != 0) && (WriteRegM == RsE)) begin
        // The execution source register mathematically collides with 
        // the pending write destination of the previous cycle.
        ForwardAE = 2'b10; // Trigger MUX to route ALUOutM backwards to ALU Input A
    end else begin
        ForwardAE = 2'b00; // Use clean baseline Register File extraction
    end
  end
endmodule
\end{verbatim}
\end{frame}

\begin{frame}{7. Control Hazards: Pipeline Penalties}
\begin{itemize}
    \item \textbf{The Problem}: The processor blindly fetches sequential code (\texttt{PC + 4}). Upon striking a branch instruction (\texttt{beq}), the true execution path is unknown until the ALU evaluates conditions inside the third EX phase.
    \item \textbf{Software Fix (See MIPS Run - Branch Delay Slot)}: Early architectures conserved transistor budgets by deliberately exposing pipelines to compilers. The hardware mechanically executed the instruction directly following the branch. Software C-Compilers had to meticulously pack this ``Delay Slot'' with safe, independent execution logic or risk corrupted state vectors.
    \item \textbf{Hardware Evolution (Predict \& Flush)}: By predicting \textit{Branch Not Taken}, the datapath optimistically marches forward. If evaluated incorrectly as \textit{Taken}, the pipeline physically halts, triggers the \texttt{reset} line on \texttt{IF/ID} bounds, and structurally collapses the rogue payload into harmless \texttt{0's} (NOP bubbles).
\end{itemize}
\end{frame}

\begin{frame}{8. Exceptions \& Interrupts}
\begin{itemize}
    \item Pipelining inherently disrupts orderly procedural progression. A division-by-zero execution fault or hardware interrupt must freeze standard datapath fetching operations immediately.
    \item The logic traps the specific active \texttt{PC} sequence inside the \texttt{EPC} (Exception Program Counter) tracking register, explicitly squashes pipeline operations locally, and drives processing towards the local OS Exception Handler boot protocol vector mapping mapping.
\end{itemize}
\end{frame}

\begin{frame}{8b. The Architect's Synthesis: Designing a Pipeline}
At the highest computational architecture level, we balance raw clock speed against physical structural constraints using three main techniques:
\begin{itemize}
    \item \textbf{Slicing the Datapath}: Physically divide ALU logic using isolating $D$-Flip-Flop boundaries to overlap instruction phases flawlessly.
    \item \textbf{Propagating Synchronous Control}: Hardware mechanically pushes dynamic Boolean multiplexer routing combinations laterally across boundary boundaries identically alongside corresponding payload execution packets.
    \item \textbf{Resolving Collisions Natively}: Deploying explicitly wired Hazard Unit blocks running concurrently with live executions to physically throw physical algorithmic bypass overrides or to manually force procedural pipeline ''Bubbles''.
\end{itemize}
\end{frame}

\begin{frame}{8c. SystemVerilog 5-Stage Pipelined Synthesis}
\begin{itemize}
    \item \textbf{Physical Codebase}: Developed within \texttt{pipelined\_cpu/}, mapping the exact MIPS32 framework against rigorous SystemVerilog execution limitations.
    \item \textbf{Register Pipeline Boundaries}: The datapath utilizes massive array-bound \texttt{always\_ff} flip-flops to discretely separate \texttt{IF/ID}, \texttt{EX/MEM}, and \texttt{MEM/WB} hardware. 
    \item \textbf{Active Structural Resolvers}: The \texttt{hazard.sv} module actively scans execution states to natively map \texttt{forwardaE} datapath shortcut overlaps, or directly forces architectural NOP bubbles (\texttt{StallD}, \texttt{FlushE}) against Load-Use collisions.
    \item \textbf{Performance Conclusion}: Single-cycle maxes at $800\text{ MHz}$ ($CPI=1.0$), Multi-cycle leaps to $3.3\text{ GHz}$ ($CPI=4.0$). Pipelining achieves the massive $5.0\text{ GHz}$ frequency while restoring an optimal theoretical $CPI=1.0$ through parallel overlap.
    \item \textbf{Run locally}: \texttt{make clean all} tracks complete output cycles automatically.
\end{itemize}
\end{frame}

\begin{frame}{9. Week 11 Summary}
\begin{itemize}
    \item \textbf{Pipeline Strategy}: Divide datapath execution linearly over discrete logical $D$-flip-flop boundary constraints natively driving isolated logic stages concurrently over absolute clock phases.
    \item \textbf{Hazard Physics}: Resolving algebraic resource overlaps necessitates explicit mathematical handling logic physically designed across tracking registers vs core ALU forwarding constraints.
    \item \textbf{Amdahl's Cap}: Hazards define the sequential ceiling $1-p$ capping overall pipeline scaling potential.
    \item \textbf{Next Session}: Extending into Advanced Interrupts and Memory Hierarchy evaluations defining system-level architectural caching logic frameworks.
\end{itemize}
\end{frame}

\end{document}
