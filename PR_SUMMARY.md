# PR Summary: Fix iverilog CAM logic warning

## Objective
The objective is to resolve an `iverilog` compilation warning (`sorry: constant selects in always_* processes are not fully supported`) generated by the fully associative cache's CAM logic during array matching.

## Intended Changes
1. Modify `cache_fully_associative.sv` to replace `always_comb` with `always @(*)` for the hit detection block.
2. Verify that `iverilog` compiles the project without any warnings.

## Implementation Details
*   Changed the sensitivity list construct from `always_comb` to `always @(*)` on line 58 of `cache_fully_associative.sv`.
*   Since `iverilog` implements stricter synthesis-like rules for `always_comb` regarding dynamic array indexing, reverting to `always @(*)` successfully silences the warning while maintaining proper combinational logic inference.
*   Ran `make compile` successfully with 0 warnings.
